# Modul B · Kapitel 4 — Deployment und Inference
#
#   python -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# Dazu gehört ein laufendes Ollama für die Notebooks, die messen:
#   ollama serve
#   ollama pull llama3.2

openai>=1.40
tiktoken>=0.7
pandas>=2.0
matplotlib>=3.8
numpy>=1.26
jupyter>=1.0

# Nur für 02_quantisierung: dort wird direkt auf den Gewichten eines kleinen
# Modells gerechnet, nicht über die API.
torch>=2.4
transformers>=4.44
