# Fine-Tuning-Kapitel, Modul B.
# Dieses Kapitel arbeitet direkt auf den Modellgewichten und braucht deshalb
# transformers und peft statt eines OpenAI-kompatiblen Clients.

torch>=2.4
transformers>=4.44
peft>=0.12
matplotlib>=3.8

# Nur für den QLoRA-Bonus, und nur auf einer CUDA-GPU (Colab).
# Auf macOS ist bitsandbytes nicht lauffähig.
# bitsandbytes>=0.43
