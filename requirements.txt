# Alle Pakete für alle Challenges. Einmal installieren, dann läuft jeder Ordner.
#
#   python3 -m venv .venv
#   source .venv/bin/activate         # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Die vollständige Anleitung steht in SETUP.md.

# Notebook-Umgebung, für alle Ordner
jupyterlab>=4.0

# Modul A · Rechnen, Daten, Diagramme
numpy>=1.26
pandas>=2.0
matplotlib>=3.8
scikit-learn>=1.4
joblib>=1.3

# Modellgewichte direkt: a4-gpt, b3-fine-tuning, b4-deployment
torch>=2.4
transformers>=4.44
peft>=0.12

# Modul B · Zugang zum Sprachmodell, über Ollama oder einen Anbieter
openai>=1.40
tiktoken>=0.7

# Modul B · b2-rag
chromadb>=1.0
langchain-text-splitters>=0.3
rank-bm25>=0.2.2

# Nur für den QLoRA-Bonus in b3-fine-tuning, und nur auf einer CUDA-GPU (Colab).
# Auf macOS ist bitsandbytes nicht lauffähig.
# bitsandbytes>=0.43
