__pycache__/
.ipynb_checkpoints/
.DS_Store
.venv/
venv/

# Wird beim Durcharbeiten erzeugt. Ohne Schrägstrich davor, damit die Muster
# auf jeder Ebene greifen — unabhängig davon, wie die Ordner heißen. Was nur
# eine einzelne Challenge betrifft, steht in deren eigener .gitignore; die
# wird mitkopiert.
chroma/
ausgabe/
